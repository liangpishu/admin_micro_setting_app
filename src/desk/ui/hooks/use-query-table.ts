import { useAntdTable, useRequest } from "ahooks";
import { CommonService } from "../../service/common/common-service";
import { useEffect } from "react";

export function useQueryTable(url: string) {
  // const { tableProps, search, params }  = useAntdTable(() => useRequest(()), {
  //   defaultPageSize: 5,
  //   form,
  // });
  //
  // useEffect(() => {
  //   if (!!tableName) {
  //     run();
  //   }
  // }, [tableName]);
  // return { data };
}
